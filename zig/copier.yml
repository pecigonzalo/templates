project_name:
  type: str
  help: Project name (use lowercase letters, digits, and hyphens only)

project_slug:
  type: str
  default: "{{ project_name | lower | replace('-', '_') | replace(' ', '_') }}"
  when: false
